language: ruby

rvm:
  - "2.4"
  - "2.5"
  - "2.6"
  - "2.7"
  - ruby-head

env:
  - COVERALL=1

cache:
  - bundler

matrix:
  allow_failures:
    - rvm: ruby-head

before_install:
  - gem install bundler
